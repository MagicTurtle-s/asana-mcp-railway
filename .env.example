# Asana OAuth Credentials
# Get these from: https://app.asana.com/0/developer-console
ASANA_CLIENT_ID=your_client_id_here
ASANA_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI
# Development: http://localhost:3000/oauth/callback
# Production: https://your-app.railway.app/oauth/callback
ASANA_REDIRECT_URI=http://localhost:3000/oauth/callback

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info

# Optional: Redis for production token storage
# REDIS_URL=redis://localhost:6379

# Optional: Session secret for cookie signing
# SESSION_SECRET=generate_random_secret_here

# Optional: CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://your-app.railway.app

# Optional: Rate limit override (default: 150 for free, 1500 for premium)
# MAX_REQUESTS_PER_MINUTE=150
